{
  "name": "project-ludo",
  "version": "1.0.0",
  "description": "AI-Powered Performance Testing Suite",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "install": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
